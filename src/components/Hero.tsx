import * as React from "react";
import { Jumbotron } from "react-bootstrap";

const Hero = () => {
  return (
    <Jumbotron>
      <h1>Music Set Builder</h1>
    </Jumbotron>
  );
};

export default Hero;
